#include "main.h"

/**
 * abs - writes the absolute value of an integer
 * @k: the integer to be written
 *
 * Return: the absolute value of int
 */
int _abs(int k)
{
	if (k >= 0)
	{
		return (k);
	}
	else
	{
		return (-k);
	}
}
